'use client';

import { createGlobLocalFilesInspector } from '@agentasia/shared-tool-ui/inspectors';

export const GlobLocalFilesInspector = createGlobLocalFilesInspector(
  'builtins.lobe-local-system.apiName.globLocalFiles',
);
