'use client';

import { createRunCommandInspector } from '@agentasia/shared-tool-ui/inspectors';

export const RunCommandInspector = createRunCommandInspector(
  'builtins.lobe-skills.apiName.runCommand',
);
